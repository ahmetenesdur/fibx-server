# Privy credentials (required)
PRIVY_APP_ID=your_privy_app_id
PRIVY_APP_SECRET=your_privy_app_secret

# JWT secret for session tokens (required)
# MUST be at least 32 characters
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# Server port (optional, defaults to 3001)
PORT=3001

# Allowed CORS origins (comma-separated, optional)
# Leave empty or unset for CLI-only access (no browser CORS needed)
# Example: ALLOWED_ORIGINS=https://app.fibx.io,http://localhost:3000
ALLOWED_ORIGINS=
